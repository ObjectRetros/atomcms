{
  "mcpServers": {
    "laravel-boost": {
      "command": "docker",
      "args": [
        "compose",
        "exec",
        "app",
        "php",
        "artisan",
        "mcp:start",
        "laravel-boost"
      ],
      "env": {
        "MCP_DEBUG": "1"
      }
    }
  }
}
